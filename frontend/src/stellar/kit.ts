import { StellarWalletsKit, Networks } from '@creit-tech/stellar-wallets-kit';

/**
 * Re-exporting for convenience and to ensure we use the same version/imports.
 */
export { StellarWalletsKit, Networks };
